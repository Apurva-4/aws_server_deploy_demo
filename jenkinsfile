pipeline {
  agent {
    kubernetes {
      namespace {
        stages{
          stage('test'){
            steps{
             kubectl get pods -n frontend
            }
          } 
        }
      }
    }
  }
}