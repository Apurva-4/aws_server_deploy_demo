pipeline {
  agent any
  stages {
    stage('Clone repository') {
      steps {
       sh 'kubectl get pods -n frontend'
      }
    }
    stage('Deploy to Kubernetes') {
      steps {
        script {
          echo "hello"
        }
      }
      }
    }
  }
